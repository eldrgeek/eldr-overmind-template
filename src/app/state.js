console.log('loading state');
export const state = {
  title: 'The application title',
  devState: {
    stateAttributes: null,
    restoreState: false,
    saveState: true,
  },
  testValue: 'testing value',
  logValues: ['a', 'b'],
};
